# Diagrammes d'Architecture

## Architecture Actuelle vs Recommandée

### 🔴 Architecture Actuelle (Problématique)

```
┌─────────────────────────────────────────────────────────────┐
│                    app/characters/[id]/page.tsx              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  13 useState                                          │  │
│  │  - character, loading, editingName, showWeaponModal  │  │
│  │  - showItemModal, showCombatSetup, currentEnemy...   │  │
│  │                                                        │  │
│  │  Logique métier mélangée:                            │  │
│  │  - handleUpdateCharacter()                            │  │
│  │  - handleAddWeapon()                                  │  │
│  │  - handleCombatEnd()                                  │  │
│  │  - updatedAt: new Date().toISOString() (répété 5x)   │  │
│  │                                                        │  │
│  │  Appels directs à IndexedDB:                         │  │
│  │  - import { getCharacter, updateCharacter } from     │  │
│  │    '@/lib/storage/characters'                        │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│           app/components/character/CharacterStats.tsx        │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  8 useState                                           │  │
│  │  - editingDexterite, dexteriteValue                  │  │
│  │  - editingChance, chanceValue                        │  │
│  │  - editingPvMax, pvMaxValue...                       │  │
│  │                                                        │  │
│  │  Logique métier répétée:                             │  │
│  │  - saveDexterite() { validate, transform, persist }  │  │
│  │  - saveChance() { validate, transform, persist }     │  │
│  │  - savePvMax() { validate, transform, persist }      │  │
│  │  - savePvActuels() { validate, transform, persist }  │  │
│  │                                                        │  │
│  │  updatedAt: new Date().toISOString() (répété 4x)     │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│              lib/storage/characters.ts                       │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Fonctions CRUD simples:                             │  │
│  │  - saveCharacter(character)                          │  │
│  │  - getCharacter(id)                                  │  │
│  │  - updateCharacter(character)                        │  │
│  │  - deleteCharacter(id)                               │  │
│  │                                                        │  │
│  │  Pas de logique métier                               │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                           ↓
                      IndexedDB

PROBLÈMES:
❌ Logique métier dispersée dans les composants UI
❌ État local excessif (21 useState dans 1 page)
❌ Duplication massive de code
❌ Impossible de tester la logique sans mocker IndexedDB
❌ Couplage fort entre UI et infrastructure
```

---

### 🟢 Architecture Recommandée (Clean Architecture)

```
┌────────────────────────────────────────────────────────────────┐
│                  🎨 PRESENTATION LAYER                          │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │         app/characters/[id]/page.tsx                      │ │
│  │  ┌────────────────────────────────────────────────────┐  │ │
│  │  │  const { character, stats, updateStats } =         │  │ │
│  │  │    useCharacter(id);                               │  │ │
│  │  │                                                      │  │ │
│  │  │  return <CharacterStatsView                        │  │ │
│  │  │    stats={stats}                                   │  │ │
│  │  │    onUpdate={updateStats}                          │  │ │
│  │  │  />                                                 │  │ │
│  │  └────────────────────────────────────────────────────┘  │ │
│  └──────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │   app/components/character/CharacterStatsView.tsx        │ │
│  │  ┌────────────────────────────────────────────────────┐  │ │
│  │  │  Composant UI PUR (affichage uniquement)          │  │ │
│  │  │  Pas d'état, pas de logique                       │  │ │
│  │  │                                                      │  │ │
│  │  │  <EditableStatField                                │  │ │
│  │  │    label="DEXTÉRITÉ"                               │  │ │
│  │  │    value={stats.dexterite}                         │  │ │
│  │  │    onSave={(v) => onUpdate({ dexterite: v })}     │  │ │
│  │  │  />                                                 │  │ │
│  │  └────────────────────────────────────────────────────┘  │ │
│  └──────────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────────┘
                              ↓
┌────────────────────────────────────────────────────────────────┐
│              🎣 PRESENTATION LOGIC (Hooks + Stores)             │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │     src/presentation/hooks/useCharacter.ts                │ │
│  │  ┌────────────────────────────────────────────────────┐  │ │
│  │  │  const [character, setCharacter] = useState(null); │  │ │
│  │  │                                                      │  │ │
│  │  │  const updateStats = async (stats) => {            │  │ │
│  │  │    const updated = await characterService          │  │ │
│  │  │      .updateCharacterStats(id, stats);             │  │ │
│  │  │    setCharacter(updated);                          │  │ │
│  │  │  };                                                 │  │ │
│  │  │                                                      │  │ │
│  │  │  return { character, stats, updateStats };         │  │ │
│  │  └────────────────────────────────────────────────────┘  │ │
│  └──────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │  src/presentation/stores/characterStore.ts (Zustand)     │ │
│  │  ┌────────────────────────────────────────────────────┐  │ │
│  │  │  characters: Map<string, Character>                │  │ │
│  │  │  loading: boolean                                  │  │ │
│  │  │                                                      │  │ │
│  │  │  updateCharacterStats(id, stats) {                 │  │ │
│  │  │    const updated = await service                   │  │ │
│  │  │      .updateCharacterStats(id, stats);             │  │ │
│  │  │    set({ characters: map.set(id, updated) });      │  │ │
│  │  │  }                                                  │  │ │
│  │  └────────────────────────────────────────────────────┘  │ │
│  └──────────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────────┘
                              ↓
┌────────────────────────────────────────────────────────────────┐
│                    🟢 APPLICATION LAYER                         │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │     src/application/services/CharacterService.ts          │ │
│  │  ┌────────────────────────────────────────────────────┐  │ │
│  │  │  constructor(repository: ICharacterRepository)     │  │ │
│  │  │                                                      │  │ │
│  │  │  async updateCharacterStats(id, stats) {           │  │ │
│  │  │    const character = await repository.findById(id);│  │ │
│  │  │    const updated = character.updateStats(stats);   │  │ │
│  │  │    await repository.save(updated);                 │  │ │
│  │  │    return updated;                                 │  │ │
│  │  │  }                                                  │  │ │
│  │  └────────────────────────────────────────────────────┘  │ │
│  └──────────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────────┘
                              ↓
┌────────────────────────────────────────────────────────────────┐
│                     🔵 DOMAIN LAYER                             │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │         src/domain/entities/Character.ts                  │ │
│  │  ┌────────────────────────────────────────────────────┐  │ │
│  │  │  class Character {                                 │  │ │
│  │  │    updateStats(newStats): Character {              │  │ │
│  │  │      const updatedStats = this.stats               │  │ │
│  │  │        .update(newStats);  // Validation incluse   │  │ │
│  │  │      return new Character(..., updatedStats, ...); │  │ │
│  │  │    }                                                │  │ │
│  │  │                                                      │  │ │
│  │  │    toData() {                                       │  │ │
│  │  │      return {                                       │  │ │
│  │  │        ...,                                         │  │ │
│  │  │        updatedAt: new Date().toISOString()  ✅     │  │ │
│  │  │      };                                             │  │ │
│  │  │    }                                                │  │ │
│  │  │  }                                                  │  │ │
│  │  └────────────────────────────────────────────────────┘  │ │
│  └──────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │         src/domain/entities/Stats.ts                      │ │
│  │  ┌────────────────────────────────────────────────────┐  │ │
│  │  │  class Stats {                                     │  │ │
│  │  │    validate() {                                    │  │ │
│  │  │      if (this.dexterite < 1)                       │  │ │
│  │  │        throw new Error('Dex >= 1');                │  │ │
│  │  │    }                                                │  │ │
│  │  │                                                      │  │ │
│  │  │    update(newStats): Stats {                       │  │ │
│  │  │      return new Stats(...);  // Immutable          │  │ │
│  │  │    }                                                │  │ │
│  │  │  }                                                  │  │ │
│  │  └────────────────────────────────────────────────────┘  │ │
│  └──────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │   src/domain/repositories/ICharacterRepository.ts         │ │
│  │  ┌────────────────────────────────────────────────────┐  │ │
│  │  │  interface ICharacterRepository {                  │  │ │
│  │  │    save(character: Character): Promise<void>;      │  │ │
│  │  │    findById(id: string): Promise<Character>;       │  │ │
│  │  │    findAll(): Promise<Character[]>;                │  │ │
│  │  │    delete(id: string): Promise<void>;              │  │ │
│  │  │  }                                                  │  │ │
│  │  └────────────────────────────────────────────────────┘  │ │
│  └──────────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────────┘
                              ↓
┌────────────────────────────────────────────────────────────────┐
│                   🔴 INFRASTRUCTURE LAYER                       │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │  src/infrastructure/persistence/                          │ │
│  │       IndexedDBCharacterRepository.ts                     │ │
│  │  ┌────────────────────────────────────────────────────┐  │ │
│  │  │  class IndexedDBCharacterRepository                │  │ │
│  │  │    implements ICharacterRepository {               │  │ │
│  │  │                                                      │  │ │
│  │  │    async save(character: Character) {              │  │ │
│  │  │      const db = await getDB();                     │  │ │
│  │  │      await db.put('characters',                    │  │ │
│  │  │        character.toData());                        │  │ │
│  │  │    }                                                │  │ │
│  │  │                                                      │  │ │
│  │  │    async findById(id: string) {                    │  │ │
│  │  │      const data = await db.get('characters', id);  │  │ │
│  │  │      return Character.fromData(data);              │  │ │
│  │  │    }                                                │  │ │
│  │  │  }                                                  │  │ │
│  │  └────────────────────────────────────────────────────┘  │ │
│  └──────────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────────┘
                              ↓
                         IndexedDB

AVANTAGES:
✅ Logique métier centralisée et testable
✅ Composants UI simples (affichage seul)
✅ Zéro duplication de code
✅ Facile de changer IndexedDB → LocalStorage ou API
✅ Tests unitaires simples (pas de mock nécessaire)
✅ État UI optimisé (Zustand = pas de re-renders inutiles)
```

---

## Flux de Données : Mise à Jour d'une Stat

### 🔴 Actuellement

```
┌──────────────────────────────────────────────────────────────┐
│  1. User clicks "Dextérité" value                            │
└──────────────────────────────────────────────────────────────┘
                        ↓
┌──────────────────────────────────────────────────────────────┐
│  2. CharacterStats.tsx                                       │
│     setEditingDexterite(true)                                │
│     setDexteriteValue(character.stats.dexterite.toString())  │
└──────────────────────────────────────────────────────────────┘
                        ↓
┌──────────────────────────────────────────────────────────────┐
│  3. User enters new value + clicks ✓                         │
└──────────────────────────────────────────────────────────────┘
                        ↓
┌──────────────────────────────────────────────────────────────┐
│  4. saveDexterite() {                                        │
│       const newValue = parseInt(dexteriteValue);             │
│       if (isNaN(newValue) || newValue < 1) {  ❌ Validation │
│         alert('La dextérité doit être...');                  │
│         return;                                              │
│       }                                                       │
│       await handleUpdateStat({                               │
│         stats: { ...character.stats, dexterite: newValue }   │
│       });                                                     │
│     }                                                         │
└──────────────────────────────────────────────────────────────┘
                        ↓
┌──────────────────────────────────────────────────────────────┐
│  5. handleUpdateStat() {                                     │
│       const updatedCharacter = {                             │
│         ...character,                                        │
│         ...updates,                                          │
│         updatedAt: new Date().toISOString()  ❌ Répété      │
│       };                                                      │
│       await onUpdate(updatedCharacter);                      │
│     }                                                         │
└──────────────────────────────────────────────────────────────┘
                        ↓
┌──────────────────────────────────────────────────────────────┐
│  6. CharacterDetail.tsx - handleUpdateCharacter()            │
│     await updateCharacter(updatedCharacter);  ❌ Direct call │
└──────────────────────────────────────────────────────────────┘
                        ↓
┌──────────────────────────────────────────────────────────────┐
│  7. lib/storage/characters.ts - updateCharacter()            │
│     const db = await getDB();                                │
│     await db.put('characters', character);                   │
└──────────────────────────────────────────────────────────────┘
                        ↓
                    IndexedDB

TOTAL: 7 étapes, logique dispersée dans 4 fichiers
```

---

### 🟢 Avec Clean Architecture

```
┌──────────────────────────────────────────────────────────────┐
│  1. User clicks "Dextérité" value                            │
└──────────────────────────────────────────────────────────────┘
                        ↓
┌──────────────────────────────────────────────────────────────┐
│  2. EditableStatField (composant réutilisable)               │
│     setEditing(true)                                         │
│     setTempValue(value.toString())                           │
└──────────────────────────────────────────────────────────────┘
                        ↓
┌──────────────────────────────────────────────────────────────┐
│  3. User enters new value + clicks ✓                         │
└──────────────────────────────────────────────────────────────┘
                        ↓
┌──────────────────────────────────────────────────────────────┐
│  4. EditableStatField.handleSave()                           │
│     onSave(newValue);  // Simple callback                    │
└──────────────────────────────────────────────────────────────┘
                        ↓
┌──────────────────────────────────────────────────────────────┐
│  5. CharacterStatsView                                       │
│     onUpdate({ dexterite: newValue });  // Simple callback   │
└──────────────────────────────────────────────────────────────┘
                        ↓
┌──────────────────────────────────────────────────────────────┐
│  6. useCharacter hook - updateStats()                        │
│     const updated = await characterService                   │
│       .updateCharacterStats(id, { dexterite: newValue });    │
│     setCharacter(updated);                                   │
└──────────────────────────────────────────────────────────────┘
                        ↓
┌──────────────────────────────────────────────────────────────┐
│  7. CharacterService.updateCharacterStats()                  │
│     const character = await repository.findById(id);         │
│     const updated = character.updateStats(stats);  ✅ Logic │
│     await repository.save(updated);                          │
│     return updated;                                          │
└──────────────────────────────────────────────────────────────┘
                        ↓
┌──────────────────────────────────────────────────────────────┐
│  8. Character.updateStats()                                  │
│     const updatedStats = this.stats.update(newStats);        │
│     return new Character(..., updatedStats, ...);            │
└──────────────────────────────────────────────────────────────┘
                        ↓
┌──────────────────────────────────────────────────────────────┐
│  9. Stats.update()                                           │
│     return new Stats(...);  // Validation automatique ✅     │
└──────────────────────────────────────────────────────────────┘
                        ↓
┌──────────────────────────────────────────────────────────────┐
│  10. Character.toData()                                      │
│      updatedAt: new Date().toISOString()  ✅ SEUL endroit   │
└──────────────────────────────────────────────────────────────┘
                        ↓
┌──────────────────────────────────────────────────────────────┐
│  11. IndexedDBCharacterRepository.save()                     │
│      await db.put('characters', character.toData());         │
└──────────────────────────────────────────────────────────────┘
                        ↓
                    IndexedDB

TOTAL: 11 étapes MAIS:
✅ Logique métier centralisée (Character + Stats)
✅ Validation automatique
✅ updatedAt géré en 1 seul endroit
✅ Composants UI réutilisables
✅ Facile à tester chaque couche séparément
```

---

## Dépendances entre Couches

```
         ╔════════════════════════════╗
         ║   🎨 PRESENTATION          ║
         ║   (app/, hooks/, stores/)  ║
         ╚═══════════╤════════════════╝
                     │ dépend de ↓
         ╔═══════════▼════════════════╗
         ║   🟢 APPLICATION           ║
         ║   (services/, use-cases/)  ║
         ╚═══════════╤════════════════╝
                     │ dépend de ↓
         ╔═══════════▼════════════════╗
         ║   🔵 DOMAIN (Core)         ║
         ║   (entities/, repositories)║  ← Ne dépend de RIEN
         ╚═══════════╤════════════════╝
                     │ ↑ implémenté par
         ╔═══════════▼════════════════╗
         ║   🔴 INFRASTRUCTURE        ║
         ║   (IndexedDB, Analytics)   ║
         ╚════════════════════════════╝

RÈGLE D'OR:
Les dépendances vont toujours vers le DOMAINE (centre)
Le DOMAINE ne dépend jamais de l'extérieur
→ Inversion de dépendance (Dependency Inversion Principle)
```

---

## Tests : Avant vs Après

### 🔴 Actuellement (Impossible de tester)

```typescript
// ❌ Comment tester CharacterStats sans mocker:
// - React
// - IndexedDB
// - alert()
// - DOM

// Impossible d'écrire un test unitaire simple
```

---

### 🟢 Avec Clean Architecture

```typescript
// ✅ Test de la logique métier (pur TypeScript)
describe('Character', () => {
  it('should update dexterity', () => {
    const character = new Character(/* ... */);
    const updated = character.updateStats({ dexterite: 10 });
    
    expect(updated.getStats().dexterite).toBe(10);
  });

  it('should throw on invalid dexterity', () => {
    expect(() => {
      new Stats(-1, 5, 5, 20, 20);
    }).toThrow('La dextérité doit être >= 1');
  });
});

// ✅ Test du service (mock du repository)
describe('CharacterService', () => {
  it('should update character stats', async () => {
    const mockRepo = {
      findById: jest.fn().mockResolvedValue(mockCharacter),
      save: jest.fn()
    };
    
    const service = new CharacterService(mockRepo);
    await service.updateCharacterStats('id-1', { dexterite: 10 });
    
    expect(mockRepo.save).toHaveBeenCalled();
  });
});

// ✅ Test du composant UI (juste l'affichage)
describe('EditableStatField', () => {
  it('should call onSave with new value', async () => {
    const onSave = jest.fn();
    render(<EditableStatField value={7} onSave={onSave} />);
    
    fireEvent.click(screen.getByText('7'));
    fireEvent.change(screen.getByRole('spinbutton'), { target: { value: '10' } });
    fireEvent.click(screen.getByTitle('Valider'));
    
    expect(onSave).toHaveBeenCalledWith(10);
  });
});
```

---

## Conclusion

L'architecture recommandée permet de:

1. **Séparer les responsabilités**
   - UI = affichage
   - Domain = logique métier
   - Infrastructure = technique (DB, API)

2. **Tester facilement**
   - Tests unitaires de la logique métier (sans React)
   - Tests d'intégration des services
   - Tests de composants UI (sans DB)

3. **Évolution facilitée**
   - Ajouter une API → changer le repository
   - Nouveau livre → ajouter des règles métier
   - Nouvelle UI → réutiliser les services

4. **Code maintenable**
   - Zéro duplication
   - Responsabilités claires
   - Facile à onboarder de nouveaux développeurs
